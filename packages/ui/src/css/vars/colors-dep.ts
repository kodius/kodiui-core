import { red, blue, green } from "@radix-ui/colors";

// todo
//  Extract all colors with some cool functions
// import * as radixColor from "@radix-ui/colors";
// const radixValues = Object.values(radixColor);
//
//  Create a interface for colors


export const colors = {
  ...red,
  ...blue,
  ...green,
};

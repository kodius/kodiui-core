{
  "name": "@kodiui/monorepo",
  "version": "0.0.10",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "docslint": "cd ./apps/docs && pnpm lint",
    "docsformat": "cd ./apps/docs && pnpm format",
    "format:fix": "cd ./apps/docs && pnpm format:fix",
    "validate": "npm-run-all --parallel docslint docsformat"
  },
  "keywords": [],
  "author": "dinoDanic",
  "license": "ISC",
  "workspaces": [
    "apps/**",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.0",
    "npm-run-all": "^4.1.5"
  }
}

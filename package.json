{
  "name": "@kodiui/monorepo",
  "version": "0.0.10",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "build:cli": "cd ./packages/cli && pnpm build",
    "validate": "npm-run-all --parallel build:cli"
  },
  "keywords": [],
  "author": "dinoDanic",
  "license": "ISC",
  "workspaces": [
    "apps/**",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.0",
    "npm-run-all": "^4.1.5"
  }
}

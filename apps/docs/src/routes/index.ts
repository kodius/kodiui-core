const guides = "/guides";
const ui = "/ui";
const components = "/components";

export const routes = {
  "new-project": `${guides}/new-project`,
  box: `${ui}/box`,
  button: `${components}/button`,
};

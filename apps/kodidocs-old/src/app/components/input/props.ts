import { PropsType } from '@features/documentation'

export const props: PropsType = {
  props: [
    {
      name: 'todo',
      values: ['todo'],
      description: 'todo',
      defaultValue: 'todo',
    },
  ],
}

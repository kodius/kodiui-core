/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ['en'],
  //   defaultLocale: "en",
  //   domains: [{ domain: "app.localhost", defaultLocale: "en" }],
  // },
}

module.exports = nextConfig

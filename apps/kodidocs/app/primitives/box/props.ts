import { PropsType } from "@/features/docs/components/Prop";

export const props: PropsType = {
  props: [
    {
      name: 'todo',
      values: ['todo'],
      description: 'todo',
      defaultValue: 'todo',
    },
  ],
}

import { Box } from "@chakra-ui/react";
import { Stack } from "@kodiui/ui";
import styled from "@emotion/styled";

export const test = () => {
  return <Stack>test</Stack>;
};

const Box = () => {
  return <Komp>hi</Komp>;
};

const Komp = styled.div``;
